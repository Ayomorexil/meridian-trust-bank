meta {
  name: 12 - Reject Transfer
  type: http
  seq: 12
}

patch {
  url: {{baseUrl}}/admin/transfers/{{transferId}}/reject
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

body:json {
  {
    "reason": "Bruno test — recipient details could not be verified"
  }
}
